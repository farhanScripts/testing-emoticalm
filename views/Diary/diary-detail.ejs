<!DOCTYPE html>
<html lang="en">
  <head>
  <%- include('../partials/head') %>
  <link rel="stylesheet" type="text/css" href="/css/diary-detail.css">
  <link rel="stylesheet" type="text/css" href="/css/responsive-detail.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Daily Journal</h1>
    <form action="/diary/<%= diaryId %>?_method=PUT" method="post">
      <div class="form-group">
        <label for="title">Title</label>
        <input
          type="text"
          class="form-control"
          id="title"
          name="title"
          value="<%= diary.title %>"
          required
        />
      </div>

      <div class="form-group">
        <label for="body">Share Your Thoughts</label>
        <textarea
          class="form-control"
          id="body"
          name="body"
          rows="5"
          required
        >
<%= diary.body %></textarea
        >
      </div>

      <div class="form-group">
        <label for="mood">Mood</label><br />
        <div class="form-check form-check-inline">
          <input
            class="form-check-input"
            type="radio"
            id="happy"
            name="mood"
            value="Happy"
            required
          />
          <label class="form-check-label" for="happy">Happy</label>
        </div>
        <div class="form-check form-check-inline">
          <input
            class="form-check-input"
            type="radio"
            id="normal"
            name="mood"
            value="Normal"
          />
          <label class="form-check-label" for="normal">Normal</label>
        </div>
        <div class="form-check form-check-inline">
          <input
            class="form-check-input"
            type="radio"
            id="sad"
            name="mood"
            value="Sad"
          />
          <label class="form-check-label" for="sad">Sad</label>
        </div>
      </div>
      <div class="button-container">
      <button type="submit" class="btn btn-primary me-2">Update</button>
    </form>
    <form action="/diary/<%= diaryId %>/delete?_method=DELETE" method="post">
      <button class="btn btn-danger">Delete</button>
    </form>
  </div>
</div>
</body>
</html>
